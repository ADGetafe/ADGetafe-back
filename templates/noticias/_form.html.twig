<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
{{ form_start(form) }}
<div class='heading-bar'>
    <div class='go-back'>
        <a href="{{ path('app_noticias_index') }}"><i class="fa-solid fa-chevron-left"></i></a>
            <div class="logo-form">
    <a
        href="/">
        <span class="logo-span">
            <img class="logo-img" src="{{ asset('images/ADG.png') }}" alt="ADG Logo"/>
        </span>
    </a>
</div>
    </div>
    <div class='acciones-btn'>
        <button class="btn btn-success">{{ button_label|default('Publicar') }}</button>
    </div>
</div>

<div class='new-form'>
    <div class="my-custom-class-for-errors">
            {{ form_errors(form) }}
    </div>

        <div class='set-foto'>
            {{ form_widget(form.foto) }}
        </div>

        <div class='set-autoria'>
            {{ form_label(form.autor, 'Autoria') }}
            {{ form_widget(form.autor) }}
        </div>

        <div class='set-categoria'>
            {{ form_label(form.categoria) }}
            {{ form_widget(form.categoria) }}
        </div>

        <div class='set-titulo-noticia'>
            {{ form_label(form.titulo, ' ') }}
            {{ form_widget(form.titulo) }}
        </div>

        <div class='set-articulo'>
            {{ form_label(form.articulo, ' ') }}
            {{ form_widget(form.articulo) }}
        </div>
{# 
        <div id="editor">
        <textarea>
        <p>Hello World!</p>
        <p>Some initial <strong>bold</strong> text</p>
        <p><br></p>
        </textarea>
        </div>
    </div> #}

</div> 
{{ form_end(form) }}

{# <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
<script src="bundles/nicolassingquill/js/nicolassing_quill.js"></script> #}

<!-- Initialize Quill editor -->
{# <script>

var herramientas = [
    ['bold', 'italic', 'underline', 'strike', {'script':'super'}, {'script':'sub'}, 'clean'],
    [{'color':[]}, {'background': []}],
    [{'align':''}, {'align':'center'}, {'align':'right'}, {'align':'justify'}],
    [{'list':'ordered'}, {'list':'bullet'}, {'indent':'+1'}, {'indent':'-1'}],
    ['link', 'image', 'video']
]
  var quill = new Quill('#editor', {
    theme: 'snow',
    placeholder: 'Escribe un Artículo',
    modules: {
        toolbar: herramientas
    }
  });


</script> #}
</body>
</html>

{# {{ form_start(form) }}
    <button class="btn btn-success">{{ button_label|default('Guardar') }}</button>
    {{ form_widget(form) }}
{{ form_end(form) }} #}

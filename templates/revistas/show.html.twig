{% extends 'base.html.twig' %}

{% block title %}Revistas{% endblock %}

{% block body %}
    <h1>Revistas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ revista.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ revista.titulo }}</td>
            </tr>
            <tr>
                <th>Revista</th>
                <td>{{ revista.revista }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ revista.createdAt ? revista.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ revista.updatedAt ? revista.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_revistas_index') }}">back to list</a>

    <a href="{{ path('app_revistas_edit', {'id': revista.id}) }}">edit</a>

    {{ include('revistas/_delete_form.html.twig') }}
{% endblock %}
